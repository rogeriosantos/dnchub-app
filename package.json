{
  "name": "dnchub",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "frontend:dev": "npm run dev --workspace=frontend",
    "backend:dev": "cd backend && uv run uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "backend:migrate": "cd backend && uv run alembic upgrade head",
    "backend:seed": "cd backend && uv run python -m app.db.seed",
    "backend:lint": "cd backend && uv run ruff check .",
    "backend:test": "cd backend && uv run pytest"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  },
  "engines": {
    "node": ">=20"
  }
}
